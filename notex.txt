productModel  =>{

}
productController =>{

}

ErrorHandling  =>{

}

userModel {


}

userController =>{

}

authentication{



}

depnedency  =>
{
cookie-parser:cookie-parser parses cookies and populates req. cookies with objects bidden to cookie names
body-parse :  body parsing middleware, which populates the req. body with (for example) the value of the parametres of a POST
bcrypt      : Bcrypt is a library to help you hash passwords. It uses a password-hashing function that is based on the Blowfish cipher
jwt : JWTs are mainly used for authentication. After a user signs in to an application, the application then assigns JWT to that user. Subsequent requests by the user will include the assigned JWT. This token tells the server what routes, services, and resources the user is allowed to access.
validator : use for validate email. is type is correct or not in schema
nodemailer : Nodemailer is a Node. js module that allows you to send emails from your server with ease. basicllay for pass reset and all
}
